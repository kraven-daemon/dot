# HOTKEY
#  command
#
# Valid modifier : super, hyper, meta, alt, control, ctrl, shift, mode_switch, lock, mod1, mod2, mod3, mod4, mod5 and any.
#  xev -event keyboard
# SXHKD_SHELL="sh"

alt + p
	dmenu_run
super + Return
	LIBGL_ALWAYS_SOFTWARE=1 alacritty	
alt + l
	lemonrun.sh
alt + k
	killall lemonbar
alt + s
	stalonetray -bg "#000000" -v --geometry +0+339 --window-type "toolbar" -i 34
alt + a 
	killall stalonetray
